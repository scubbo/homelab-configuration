apiVersion: v1
kind: Service
metadata:
  name: {{ include "pl8calcul8.fullname" . }}
  labels:
    {{- include "pl8calcul8.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pl8calcul8.selectorLabels" . | nindent 4 }}
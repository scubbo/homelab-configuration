apiVersion: v1
kind: Service
metadata:
  name: {{ include "wedding-media.fullname" . }}
  labels:
    {{- include "wedding-media.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wedding-media.selectorLabels" . | nindent 4 }}
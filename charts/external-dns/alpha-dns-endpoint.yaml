apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: alpha
  namespace: external-dns
spec:
  endpoints:
    - dnsName: alpha.avril
      recordType: A
      targets:
        - 192.168.1.219
